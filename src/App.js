var React = require('react/addons');
var cx = React.addons.classSet;

var App = React.createClass({

  render: function() {
    return (
      <div>
        here is App
      </div>
    );
  }

});

module.exports = App;